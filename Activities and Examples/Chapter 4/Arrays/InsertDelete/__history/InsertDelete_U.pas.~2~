unit InsertDelete_U;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, ExtCtrls;

type
  TForm5 = class(TForm)
    Panel1: TPanel;
    Button1: TButton;
    GroupBox1: TGroupBox;
    cmbInsert: TComboBox;
    btnInsert: TButton;
    GroupBox2: TGroupBox;
    ComboBox2: TComboBox;
    btnDelete: TButton;
    procedure GroupBox2Click(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form5: TForm5;
  iArrNumbers: array[1..10] of integer=(6,9,2,1,7);
  iSize : integer =5;

implementation

{$R *.dfm}

end.
