unit Replace_U;

interface

uses
  Windows, Messages, SysUtils, Variants, Classes, Graphics, Controls, Forms,
  Dialogs, StdCtrls, ExtCtrls;

type
  TForm5 = class(TForm)
    Panel1: TPanel;
    edtSentence: TEdit;
    Label1: TLabel;
    btnVowels: TButton;
    edtReplace: TEdit;
    procedure btnVowelsClick(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  Form5: TForm5;

implementation

{$R *.dfm}

procedure TForm5.btnVowelsClick(Sender: TObject);
var
sSentence, sReplaceAll : string;

begin
 sSentence := edtSentence.Text;
 sReplaceAll :=
end;

end.
